export interface userSignupPayload {
    firstName: string;
    lastName: string;
    phoneNumber: string;
    email: string;
    userName: string;
    password: string;
}

export interface userLoginPayload {
    userName: string;
    password: string;
}

export interface userAddPost {
    
}